    x   store values
    x   call functions
    x   pass arguments
    x   return values
    x   expressions
    x   recurtion
    x   if loops
    x   while loops
    x	  for loops 
    x   Non local Vaiables


        arrays
            access
            setting values
        floating point
        divion
		  dangling else
